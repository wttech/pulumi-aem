name: pulumi-provider-aem
runtime: yaml
plugins:
  providers:
    - name: aem
      path: ../../bin

resources:
  myModel:
    type: aem:compose:InstanceResourceModel
    properties:
      length: 24

outputs:
  output:
    value: ${myModel.result}
